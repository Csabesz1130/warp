name: TargetGraph NYX Overnight
description: Run the TargetGraph NYX automation profile from Warp.
command: |
  bash integrations/targetgraph/tg-warp nyx --profile {{profile}}
arguments:
  - name: profile
    description: Training profile.
    default: standard
